<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aguirre English Studio | Clases Interactivas</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #1e2219; /* Oliva profundo */
            --card-bg: #2a2f24;  /* Oliva suave texturizado */
            --text-main: #f4f3ef; /* Blanco cálido */
            --text-muted: #a8a7a1; /* Gris suave */
            --accent: #d4a373;    /* Bronce / Dorado otoñal */
            --accent-hover: #e9c46a;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            padding: 2rem 1rem;
        }

        .container {
            max-width: 800px;
            width: 100%;
            margin: 0 auto;
            flex: 1;
        }

        header {
            text-align: center;
            margin-bottom: 3.5rem;
            padding-top: 1rem;
        }

        header h1 {
            font-weight: 600;
            font-size: 2.2rem;
            letter-spacing: -0.5px;
            margin-bottom: 0.5rem;
            color: var(--text-main);
        }

        header p {
            color: var(--text-muted);
            font-weight: 300;
            font-size: 1.1rem;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        .card {
            background-color: var(--card-bg);
            border-radius: 12px;
            padding: 1.8rem;
            border: 1px solid rgba(255, 255, 255, 0.03);
            transition: transform 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .card:hover {
            transform: translateY(-4px);
            border-color: rgba(212, 163, 115, 0.3);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .card-content h2 {
            font-size: 1.3rem;
            font-weight: 500;
            margin-bottom: 0.5rem;
            color: var(--text-main);
        }

        .card-content p {
            color: var(--text-muted);
            font-size: 0.95rem;
            font-weight: 300;
            margin-bottom: 1.5rem;
        }

        .btn {
            display: inline-block;
            text-align: center;
            background-color: transparent;
            color: var(--accent);
            border: 1px solid var(--accent);
            padding: 0.6rem 1.2rem;
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.2s ease;
        }

        .btn:hover {
            background-color: var(--accent);
            color: var(--bg-color);
        }

        footer {
            text-align: center;
            margin-top: 4rem;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            color: var(--text-muted);
            font-size: 0.85rem;
            font-weight: 300;
        }
    </style>
</head>
<body>

    <div class="container">
        <header>
            <h1>Aguirre English Studio</h1>
            <p>Clases Interactivas y Material de Estudio</p>
        </header>

        <main class="grid">
            
            <div class="card">
                <div class="card-content">
                    <h2>Clase 1: Introducción & Fundamentos</h2>
                    <p>Revisión de estructuras esenciales, vocabulario clave y primeros ejercicios prácticos interactivos.</p>
                </div>
                <a href="clase1/index.html" class="btn">Acceder a la Clase</a>
            </div>

            <div class="card">
                <div class="card-content">
                    <h2>Clase 2: Comprensión y Fluidez</h2>
                    <p>Estrategias avanzadas de listening, speaking y actividades con botones interactivos integrados.</p>
                </div>
                <a href="clase2/index.html" class="btn">Acceder a la Clase</a>
            </div>

        </main>

        <footer>
            <p>&copy; 2026 Aguirre English Studio. Todos los derechos reservados.</p>
        </footer>
    </div>

</body>
</html>